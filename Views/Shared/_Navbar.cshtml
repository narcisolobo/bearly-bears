<nav class="navbar navbar-expand-lg navbar-dark bg-dark-100 shadow-lg">
    <div class="container">
        <a class="navbar-brand fs-3" asp-action="Posts" asp-controller="Posts">The Wall</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="ms-auto d-flex justify-content-between align-items-center gap-3">
                @if (Context.Session.GetString("username") is not null)
                {
                    <p class="m-0 navbar-text">@Context.Session.GetString("username")</p>
                }
                <form asp-action="Logout" asp-controller="Users" method="post">
                    <input type="submit" value="LOGOUT" class="btn btn-sm btn-danger text-light">
                </form>
            </div>
        </div>
    </div>
</nav>